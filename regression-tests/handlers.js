const { rest } = require('msw');

// No handlers yet; scaffold for future API mocks
const handlers = [
  // Example:
  // rest.get('/api/example', (req, res, ctx) => {
  //   return res(ctx.json({ ok: true }));
  // }),
];

module.exports = { handlers, rest };
